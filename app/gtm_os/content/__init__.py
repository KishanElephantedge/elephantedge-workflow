"""Content Intelligence -- a first-class, parallel consumer of the shared GtmSignal sensing
layer (see content-intelligence-architecture.md, Step 16), never downstream of Problem/Demand.

Populated one piece at a time, same discipline as every other app/gtm_os/ sub-package:
- topics.py    Source-agnostic canonical topic/alias configuration (Step 16A). Foundation only
               -- no topic extraction, clustering, trend detection, or sensing yet."""
